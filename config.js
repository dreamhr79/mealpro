// Runtime configuration for public client values only.
// Do NOT put Supabase service_role keys or other server secrets here.
// The anon/publishable key is intentionally a client credential and is protected by RLS.
window.MEALPRO_CONFIG = window.MEALPRO_CONFIG || {
  supabaseUrl: '',
  supabaseAnonKey: ''
};
